# Development overrides
version: '3.8'

services:
  app:
    build:
      target: builder
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    ports:
      - "5000:5000"

  caddy:
    ports:
      - "8080:80"
